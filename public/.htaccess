<IfModule mod_rewrite.c>
    RewriteEngine On

    # Si le fichier existe, on le sert directement (CSS, JS, images)
    RewriteCond %{REQUEST_FILENAME} -f
    RewriteRule ^ - [L]

    # Sinon, on redirige vers index.php
    RewriteRule ^ index.php [L]
</IfModule>
